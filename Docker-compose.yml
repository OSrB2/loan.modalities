name: loanModalities
services:
  pg:
    image: postgres:15-alpine
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=loanModalities
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: